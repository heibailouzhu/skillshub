[default]
# 输出格式：支持 Html, Xml, Lcov, Json
out = ["Html", "Lcov"]

# 输出目录
output-dir = "target/tarpaulin"

# 是否运行测试
run-types = ["Tests"]

# 覆盖率阈值（可选）
fail-under = 75

# 工作线程数（可选，默认为 CPU 核心数）
# threads = 4

# 超时时间（可选，单位：秒）
# timeout = 300
